class treeNode {
	constructor(id, value, question, leftNode, rightNode) {
		this.id = id;
		this.value = value;
		this.question = question;
		this.leftNode = leftNode;
		this.rightNode = rightNode;
	}
}
